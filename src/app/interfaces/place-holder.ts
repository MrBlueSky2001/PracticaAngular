export interface PlaceHolder {
    userId: number;
    id:     number;
    title:  string;
    body:   string;
}

export interface PlaceHolderP {
    id: number,
    usuario: string,
    email: string,
    modulo: string,
    horas: number,
    foto: string
}

export interface PlaceHolderA {
    id: number,
    usuario: string,
    email: string,
    edad: number,
    foto: string
}